# Retrospective log

Running changelog of meta-improvements made by the `retrospective` agent —
edits to CLAUDE.md, agent files under `.claude/agents/`, and `scripts/`.

Format: `YYYY-MM-DD — <change> — <reason>`
